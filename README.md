# Install vcpkg
```sh
$ git clone https://github.com/Microsoft/vcpkg.git # To any directory (recommended home directory) (cd ~)
$ cd vcpkg
$ ./bootstrap-vcpkg.sh
```

```sh
$ mkdir build

$ cmake ..

$ cmake --build .

$ ./weehub
```